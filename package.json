{
    "name": "func-js",
    "version": "0.1.0",
    "description": "The TON FunC smart-contract compiler",
    "main": "dist/index.js",
    "author": "TonTech",
    "license": "MIT",
    "private": "false",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/the-ton-tech/ton-compiler.git"
    },
    "scripts": {
        "build": "tsc && yarn copy:wasmlib",
        "test": "jest",
        "copy:wasmlib": "rm -rf ./dist/wasmlib && mkdir ./dist/wasmlib && cp -r ./src/wasmlib/* ./dist/wasmlib/",
        "release": "rm -fr dist && tsc && yarn copy:wasmlib && jest && yarn publish"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/jest": "^24.0.23",
        "jest": "^26.4.2",
        "ts-jest": "^26.4.0",
        "typescript": "^3.7.2",
        "ton": "^11.18.2"
    }
}